{
  "name": "@mrp/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Android Runtime MVP desktop control panel",
  "author": "Android Runtime MVP Team",
  "main": "build/main.js",
  "scripts": {
    "dev": "npm run build && electron .",
    "build": "npm run build:native:win && tsc -p tsconfig.json && npm run copy:renderer",
    "build:native:win": "node scripts/build-native-win.js",
    "copy:renderer": "copyfiles -u 2 \"src/renderer/**/*\" build/renderer",
    "pack": "npm run build && electron-builder --dir",
    "dist": "npm run build && electron-builder",
    "dist:win": "npm run build && electron-builder --win nsis portable",
    "dist:linux": "npm run build && electron-builder --linux AppImage deb",
    "start": "npm run dev"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "copyfiles": "^2.4.1",
    "electron": "35.1.5",
    "electron-builder": "^24.13.3",
    "node-addon-api": "^8.2.0",
    "node-gyp": "^11.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  }
}
